<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "autenticacion">
    
    <selecte id = "loginEscritorio" resultType = "modelo.pojo.Medico" parameterType = "map">
        SELECT idMedico, nombre, apellidoPaterno, apellidoMaterno, fechaNacimineto, sexo, numeroDePersonal, numeroCedulaProfecional, rol, domicilio, fotografia
        FROM medico
        WHERE numeroDePersonal = #{numeroDePersonal} AND password = #{password}
    </selecte>
    
</mapper>